<div class="container mt-5">
  <h1 class="text-center mb-4">📚 Wiki Anonyme</h1>

  <div class="d-flex justify-content-between mb-3">
    <h2 class="h4">Liste des articles</h2>
    <%= link_to '➕ Nouvel article', new_article_path, class: 'btn btn-primary' %>
  </div>

  <table class="table table-hover shadow-sm">
    <thead class="table-dark">
      <tr>
        <th scope="col">Titre</th>
        <th scope="col" class="text-center">Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @articles.each do |article| %>
        <tr>
          <td>
            <%= link_to article.title, article, class: "text-decoration-none text-dark fw-bold" %>
          </td>
          <td class="text-center">
            <%= link_to edit_article_path(article), class: "btn btn-warning btn-sm mx-1" do %>
              <i class="fas fa-edit"></i> Modifier
            <% end %>
            <%= button_to 'Supprimer', article, method: :delete, data: { confirm: 'Es-tu sûr ?' }, class: 'btn btn-danger btn-sm' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
